import { Button, ButtonGroup } from "@nextui-org/react";
const AdvListTypeSelector = () => {
  return (
    <ButtonGroup>
      <Button variant="flat">Новое</Button>
      <Button>Рекомедованное</Button>
    </ButtonGroup>
  );
};

export default AdvListTypeSelector;
